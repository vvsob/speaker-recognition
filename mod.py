import random

class Model:
    """Dummy Model class till the main one is ready"""
    def __init__(self):
        pass

    def predict(self, array, sampling_rate):
        return str(random.randint(0, 2))